<html>
<head>
    <title>Jungle Library</title>
</head>
<body>
    <h1>Jungle library</h1>
    <h2>Books</h2>

    {% if books is not empty %}
        <p>Here are all your books:</p>
        <ul>
            {% for book in books %}
                <li><a href="/books/{{ book.getId }}">{{ book.getTitle }}</a></li>
            {% endfor %}
        </ul>
    {% endif %}

    <form action='/create/books' method='post'>
        <label for='title'>Book Title:</label>
        <input id='title' name='title' type='text'>

        <button type='submit'>Add New Book:</button>
    </form>
    <form action='/delete_books' method='post'>
        <button type='submit'>Clear all Books</button>
    </form>

    <p><a href='/'>Home</a></p>
</body>
</html>
